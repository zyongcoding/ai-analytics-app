# Build Stage: Compile the React app using Node
FROM node:20-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

# Production Stage: Serve the static files using Nginx
FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
# Expose port 80 for Azure
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]